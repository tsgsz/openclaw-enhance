37:The Orchestrator discovers and selects workers dynamically from their `AGENTS.md` frontmatter:
40:2. **Parse**: Extract routing metadata (capabilities, constraints, cost) from YAML frontmatter to build candidate catalog
52:**Note**: Worker capabilities are defined in their respective `AGENTS.md` frontmatter, not in this list. This section provides examples only; the Orchestrator must discover actual worker metadata at runtime.
76:The Orchestrator uses a **bounded multi-round loop** to handle complex tasks that require iterative refinement. This replaces the previous one-shot fan-out/fan-in model with an event-driven approach using `sessions_yield` as the round-boundary synchronization primitive.
95:4. **YieldForResults**: Call `sessions_yield` to end current turn cleanly
115:| `max_rounds` | Maximum allowed rounds (default: 3, hard cap: 5) |
144:4. **Yield**: Call `sessions_yield` to await recovery results.
152:- **`sessions_yield`**: End orchestrator turn to await auto-announced results
155:**Important**: `sessions_yield` is used ONLY by the orchestrator at round boundaries. Workers remain single-round executors and do not use yield.
