QA Scenario: Register and retrieve a project
Date: 2026-03-18
Task: 2 — Registry Persistence with Atomic Writes

Test Command:
  python -m pytest tests/unit/test_project_registry.py::test_register_and_get_roundtrip tests/unit/test_project_registry.py::test_list_projects_returns_all_registered tests/unit/test_project_registry.py::test_list_projects_filters_by_kind -v

Result: ALL PASSED (3/3)

Evidence:
  - test_register_and_get_roundtrip: Creates ProjectRegistry with tmp path, registers ProjectInfo, gets by path, asserts name/type/kind match and canonical key returned.
  - test_list_projects_returns_all_registered: Registers 3 projects, list_projects() returns all 3 with correct names.
  - test_list_projects_filters_by_kind: Registers permanent + temporary projects, filters correctly by kind.

Additional tests that also passed:
  - test_canonical_path_dedup: Same directory registered twice (different name) → only 1 entry, last-write-wins.
  - test_atomic_save_cleans_up_tmp: After save(), .tmp file is cleaned up, registry file contains valid JSON.
  - test_v1_to_v2_migration: v1 format migrated to schema_version=2, projects as dict.
  - test_is_stale_detects_modified_indicator: Fresh → not stale, after modifying indicator → stale.
  - test_missing_registry_file_creates_empty_state: Nonexistent path → empty state, no error.

Full test run: 9 passed in 0.08s
