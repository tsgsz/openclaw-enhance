68:The Orchestrator uses a **bounded multi-round loop** to handle complex tasks that require iterative refinement. This replaces the previous one-shot fan-out/fan-in model with an event-driven approach using `sessions_yield` as the round-boundary synchronization primitive.
87:4. **YieldForResults**: Call `sessions_yield` to end current turn cleanly
95:- **Recovery Dispatch**: Tool-usage failure detected; dispatch `oe-tool-recovery`
110:| `blocked_items` | Issues requiring external intervention |
115:| `termination_state` | One of: `active`, `completed`, `blocked`, `exhausted`, `escalated` |
124:- **Blocker escalation**: If two consecutive evaluations show no progress, terminate as `blocked`.
125:- **Recovery Cap**: Max ONE recovery-assisted retry per failed step.
135:3. **Recovery Dispatch**: Spawn `oe-tool-recovery` via `sessions_spawn` with the failed context.
136:4. **Yield**: Call `sessions_yield` to await recovery results.
144:- **`sessions_yield`**: End orchestrator turn to await auto-announced results
147:**Important**: `sessions_yield` is used ONLY by the orchestrator at round boundaries. Workers remain single-round executors and do not use yield.
156:- `blocked`: Requires main decision or intervention
1452:3. **Yield for Results**: Call `sessions_yield` to cleanly end turn
1456:**Important**: After calling `sessions_yield`, wait for auto-announced results. Do not poll or query session state while waiting.
1477:| **Tool-Usage Failure** | tool_not_found, invalid_parameters, permission_denied, tool_execution_error | Route to oe-tool-recovery (max 1 attempt per failed step) |
1479:| **Escalated** | Design conflict, needs main decision | Yield `blocked` checkpoint to main |
1483:When a **Tool-Usage Failure** (tool-usage failure) is detected, the Orchestrator dispatches to `oe-tool-recovery` to generate a `RecoveredMethod` (recovered_method).
1492:1. **Dispatch**: Orchestrator spawns `oe-tool-recovery` with the failure context via `sessions_spawn`.
1493:2. **Yield**: Orchestrator calls `sessions_yield` to await the recovery suggestion.
1496:5. **Reroute/Escalate**: If `retry_owner` is `reroute`, `blocked`, or `escalated`, the Orchestrator follows the corresponding path.
1499:- **Max 1 recovery-assisted retry** per failed step.
1508:- **Recovery**: `oe-tool-recovery` identifies that `websearch_web_search_exa` should be used instead.
1513:- **Recovery**: `oe-tool-recovery` reads the file and provides the exact `oldString` with correct indentation.
1517:- **Signal**: `oe-tool-recovery` cannot find a solution or the assisted retry fails.
1526:- `blocked`: External decision required
